/**
 * Move controller module - controls the transform fields in SVG entity elements and manages
 * mouse event handler updates as well as which element is selected
 */
var Loader = (function () {

	//Private vars and methods
	
	//...

	//Start of privileged methods
	return {
		
    	/**
    	 * Called when SVG document is loaded (ie set as onLoad handler)
    	 * @param evt
    	 */
    	onDocumentLoad : function (evt) {
    		
    		
    		//document.oncontextmenu = WebgraphApplication.onContextMenu;
    		//dataModel.getRelationships(svgContentController.onRelationshipsRetrieved);
    		//WebgraphApplication.DataModel.getEntitiesAsync(WebgraphApplication.SVGDOMProxy.onEntitiesRetrieved);
    	}
	
	};
})();